# 데이터는 git 에 올리지 않는다 (전송은 kubectl cp, 보관은 로컬/DDN).
# .gitkeep 만 추적해서 디렉토리 자체는 clone 에 남긴다.
rl-dataset/*
!rl-dataset/.gitkeep
claude_logs
wandb
.claude
checkpoints
out
sbatch

# python
.venv/
__pycache__/

# 라운드 메일박스 (프레임워크가 만들고 관리하는 상태)
runs/
