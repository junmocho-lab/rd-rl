# test-time Q guidance — 연구 노트

2026-09-01/02 논의 정리. 실행 기록은 `RUNS.md`, 전체 결과표는 `RESULTS.md`.

---

## 1. 확실해진 것

**고정 씬에서 test-time Q guidance 가 크게 작동한다.** 200 에피소드, 같은 장면 페어 비교:

| | BC | sel32 (선택만) | parl (선택+∇Q 상승) |
|---|---|---|---|
| **A** (8,2) 90차원 | 61.5% | 76.0% (p=0.0025) | **85.0%** (p=1e-6) |
| **B** (20,5) 225차원 | 72.5% | 81.5% (p=0.054) | **98.0%** (p~1e-14) |
| **C** (20,5) 랜덤씬 | 48.6% | 56.0% (ns) | 63.0% (p=0.061) |

B 의 98.0% 는 `all` critic @200K, gm 0.05. 승/패 **54:3** 이다.

**parl > sel32 가 일관된다.** A 44:26 (p=0.041), B 37:20 (p=0.033). B 에서는 sel32 가 10개
스텝 전부 무의미했는데 parl 은 p=1e-4 였다 — selection 은 후보 32개 안에 갇혀 있고
(RTC prefix 가 후보 산포를 0.019→0.0047 로 누른다) ascent 는 그 밖으로 나간다.

**속도도 같이 개선된다.** 성공길이 parl 173~233 / sel32 194~229 / BC 206~252.
둘 다 성공한 장면만 봐도 arm 이 67% 에서 더 빠르다. 성공만으로 학습한 critic 의 참값이
γ^(T-t) 라 "빨리 끝나는 것" 을 높게 보는 효과다.

---

## 2. 답하지 못한 것 — 그리고 왜

| 질문 | 상태 |
|---|---|
| critic 몇 스텝이 적당한가 | **미해결.** A 20K 최고 / B 20K·200K 좋고 100K 만 골짜기 / C 평평 |
| success only vs all | **미해결.** 23쌍 직접 비교에서 유의 3쌍, 방향 반대 |
| 과적합이 해로운가 | **미해결.** B 100K 는 −17pp 인데 200K 는 정상 |

**근본 원인: 스텝 스캔 20개를 전부 sel32 로 했다.** 나중에 sel32 가 열등한 계측기로
판명되었으므로(위) 그 곡선들은 "critic 품질" 이 아니라 "후보 다양성이 언제 덜 부족한가" 를
잰 것일 수 있다. parl 로 스텝을 훑은 것은 B(4점)와 C(3점)뿐이다.

**교훈:** 계측기를 먼저 검증하고 스캔해야 한다. 우리는 스캔을 먼저 하고 계측기를 나중에
의심했다.

---

## 3. guide_move — 좁고 세팅 의존적인 창

B, critic success@20K, 100ep:

```
gm      0      0.001   0.005   0.01   0.02   0.05    0.1     0.2
      69.0%   61.0%   73.0%   79.0%  79.0%  91.0%   96.0%   28.0%
      (18/19) (18/27) (23/20) (23/14)(22/13)(51/14) (29/3)  (9/51) ⚠
```

**0.1 까지 단조 증가해서 96.0% (승/패 29:3), 0.2 에서 28.0% 로 절벽.** 완만한 하락이 아니라
임계값이 있는 것처럼 무너진다.

세팅별 안전 상한이 **10~20배 다르다**:

```
A  0.05 에서 85.0%    (0.1 이상 미측정)
B  0.1  에서 96.0%    0.2 에서 붕괴
C  0.01 에서 63.0%    0.05 에서 붕괴 (29.0%, p=0.0008)
```

`gm=0` 은 상승 폭이 0 이라 후보가 안 바뀌고 top-10 의 argmax = 전체 argmax 이므로
**parl ≡ sel32** 다. B 에서 69.0% 가 나와 sel32 의 63.5%(200ep)와 맞았다 — 파이프라인
정합성 검사 통과.

---

## 4. 핵심 관찰 — 노이즈 바닥이 42%다

`gm=0` 과 `sel32` 는 **수학적으로 같은 알고리즘**인데 같은 장면끼리 비교하면:

```
gm=0 69.0%  vs  sel32 67.0%     불일치 42/100 = 42%,  승/패 22:20,  p=0.878
```

**같은 알고리즘을 두 번 돌리면 장면의 42% 에서 결과가 뒤집힌다.** 정책이 diffusion 이라
같은 상태에서도 매번 다른 액션을 뽑기 때문이다. 즉:

- "장면 k 에서 BC 가 성공했다" = **한 번 뽑아서 성공했다**
- arm 이 그 장면에서 실패한 것은 guidance 가 해쳤다는 증거가 **아니다**

이 바닥을 알면 결과 해석이 바뀐다:

```
노이즈 바닥            22:20   (대칭)
parl gm=0.01 vs BC    23:14   (약간 승)
parl all@200K vs BC   54:3    ← 바닥이 20:20 인데 3번만 졌다
```

**54:3 이 놀라운 이유가 여기 있다.** "이기기만 한다" 가 아니라 **BC 가 성공하던 장면을
거의 하나도 안 놓쳤다** 는 뜻이고, 확률적 정책에서 그러려면 guidance 가 실패 draw 를
건져 올리고 있어야 한다.

**빠진 대조군:** BC 를 같은 씬에 다시 돌린 것이 세 세팅 모두 없다. 위 42% 는 B 의 sel32
계열끼리 잰 값이다. `METHOD=bc` 로 세 세팅에서 200ep 씩 재면 세팅별 바닥이 나오고,
C 는 랜덤씬 시드 정렬 검증까지 겸한다.

---

## 5. 실패의 성격 — 이 태스크는 이진적이다

C `parl gm=0.01 success@1K` 의 100 장면을 BC 와 나란히 본 결과
(`.../parl_gm001__success@1k/compare_w_bc/`):

| 구분 | n | arm 길이 | BC 길이 | arm 깊이 | BC 깊이 |
|---|---|---|---|---|---|
| 이김 | 20 | 206 | **360** | 0.0479 | 0.0132 |
| 짐 | 9 | **360** | 253 | 0.0115 | 0.0488 |
| 둘다성공 | 43 | 205 | 221 | 0.0489 | 0.0487 |
| 둘다실패 | 28 | **360** | **360** | 0.0076 | 0.0075 |

**모든 실패가 360프레임 타임아웃이고, 실패 시 못 깊이는 중앙값 0.0000** (임계 0.04).
"아깝게 실패" 가 거의 없다. critic 이 바꾸는 것은 궤적의 정밀도가 아니라 **에피소드가
궤도에 오르는가** 다.

**둘 다 실패한 28개를 제외하면 arm 87.5% / BC 72.2% (+15.3pp)** — B 의 개선폭과 같다.
"C 에서는 critic 이 안 된다" 가 아니라 **"C 에는 아무것도 안 되는 장면이 28% 섞여 있다"**
로 읽을 수 있다. 다만 불일치 쌍은 그대로이므로 p 값(0.061)은 변하지 않는다.

**궤적은 늦게 갈린다.** EEF 위치가 2cm 벌어지는 시점이 이김 91 / 짐 83 / 둘다 115 프레임
(replan 20 이므로 4~5번째 재계획). 첫 결정에서 바로 갈리는 경우는 드물다.

---

## 6. 연구 방향 — 상태 적응 온도

### 문제 설정

Q 가중 정책 개선의 목표 분포는 공통적으로

```
π*(a|s) ∝ π(a|s) · exp( Q(s,a) / τ )
```

이고 **τ 가 고정 스칼라다**:

| 방법 | 온도 | 층위 |
|---|---|---|
| **QAM** (`qam.py:85`) | `inv_temp = 0.3` 고정. `adj = -∇_a Q · inv_temp` | 학습 시점 (adjoint matching 으로 velocity field 수정) |
| **PA-RL** | `Categorical(logits=Q)` — 온도 1 암묵 고정, Q 스케일이 온도 역할 | 추론 시점 |
| **우리** | `guide_move` 고정 | 추론 시점 |

τ 가 크면 π 에 가깝고(멀티모달·실패 모드 유지), 작으면 argmax_a Q 에 가깝다(모드 집중,
critic 오차에 취약). **최적 τ 는 π(a|s) 의 상태별 모양에 달려 있고, 우리 실측에서 그
최적점이 세팅마다 10~20배 달랐다** (§3).

### 왜 추론 시점이 유리한가

QAM 의 `inv_temp` 는 한 번 정하면 정책에 새겨져 배포 시 조절할 수 없다. 추론 시점 방법은
매 결정마다 신호를 관측하므로 되먹임이 가능하다. 그리고 **우리는 이미 그 신호들을
계산해서 로깅만 하고 있다**:

| 신호 | 코드 | 뜻 |
|---|---|---|
| `candidate_q_std` | `vla_rldx.py:658` | 후보 32개의 Q 산포 — **얻을 것이 있는가** |
| `ens_std_pre` | `:644` | 편집 **전** 앙상블 불합의 — 이 상태가 이미 OOD 인가 |
| `guide_ens_std` | `:659` | 편집 **후** 앙상블 불합의 — guidance 가 밀어냈는가 |

코드 주석이 앞의 둘을 이미 구분해 놓았다: *"편집 전 불일치가 이미 크면 그 상태 자체가
학습 분포 밖이고, 편집 후에만 커지면 guidance 가 밀어낸 것이다 — 원인이 다르다."*

### 구상

**현재:** `--ood-gate` 는 편집 **전** std 로 판단하는 상태 단위 이진 게이트 (편집을 하거나
안 하거나). 우리 실험에서 한 번도 켜지 않았다.

**제안 A — 신뢰영역 조기 정지.** `_cog_guide` 의 keep-best 판정에 불확실성 항을 더한다:

```python
take = (qq > bq) and (std < 임계)      # 지금은 (qq > bq) 뿐
```

그러면 `guide_move` 가 "고정 이동량" 에서 **"안전 상한"** 으로 바뀐다. 상태마다 갈 수
있는 만큼 가고 위험해지면 멈추므로, §3 의 "세팅마다 10~20배 다른 최적값" 문제가 사라진다.
크게 잡아도 알아서 멈춘다.

부수 효과: **`guide_steps` 가 처음으로 실질 하이퍼파라미터가 된다.** 현재 총 이동거리는
`guide_move × √d` 로 스텝 수와 무관하고 (`step = guide_move·√d / steps`), 스텝 수는 경로
해상도일 뿐이다. 조기 정지가 들어가면 "얼마나 잘게 쪼개서 위험을 감지하나" 가 된다.

**제안 B — 신호로 스텝 크기 변조.** `step ∝ 1/(1 + β·std)`. A 보다 매끄럽지만
하이퍼파라미터 β 가 늘고 std 스케일에 의존한다.

### 선행 조건 — 신호가 실재하는지 먼저

지금 앙상블 std 는 **쓸 수 없다.** 헤드가 2개(clipped double Q)뿐이라 std 가 사실상 두 값의
차이다. `probe_actsens` 실측:

```
critic          logged std    shuffled 액션에서
A success@20K    0.0002        0.0008  (3.2배)
B success@1K     0.0002        0.0007  (3.2배)
C success@1K     0.0004        0.0007  (1.9배)
```

**REDQ 앙상블(10헤드)이 있어야 신호가 생긴다.** C 에 `success_ens` / `all_ens` critic 이
이미 학습돼 있으나 **평가를 한 번도 하지 않았다.** 이것이 현재 가장 값어치 있는 빈칸이다.

### 검증 순서

1. **신호 검증** — C 의 앙상블 critic 에 `probe_actsens`. shuffled 에서 std 가 10배 이상
   오르는가. 롤아웃 불필요, 15분. **여기서 실패하면 제안 A·B 는 만들어도 작동하지 않는다.**
2. **`guide_steps` 재측정** — `probe_steps.py` 를 90차원 critic 으로. Q 가 국소 선형이면
   조기 정지의 해상도 이득이 작다. 롤아웃 불필요, 10분.
3. **BC 재실행 3개** — 세팅별 노이즈 바닥 (§4). C 는 시드 정렬 검증도 겸한다.
4. **구현 + 테스트** — B 의 gm 0.1↔0.2 절벽에서 신뢰영역이 성능을 지켜주는지.
   B 를 쓰는 이유: BC 를 크게 넘는 세팅에서 gm 민감도를 없애는 것이 방법의 값어치다.
   C 는 어떤 조합에서도 BC 를 못 넘었으므로 거기서 막아봐야 BC 수준으로 돌아오는 것이다.

---

## 7. 열린 질문 — 멀티모달성은 조절 가능한가

**같은 장면에서 성공/실패가 반복되는 것**(§4 의 42%)이 위 신호들로 고쳐질 문제인가.

B 는 **된다.** gm 0.1 에서 96.0%, 승/패 29:3 — BC 가 실패한 30개 중 29개를 건졌다.
노이즈 바닥 42% 가 3% 로 줄었다. 충분히 밀면 성공 모드로 집중된다.

C 는 **모른다.** 최고가 63% 이고 어떤 gm·스텝·필터 조합도 BC 를 유의하게 넘지 못했다.
두 경우가 완전히 다른 처방을 요구한다:

```
후보 32개 안에 성공 액션이 있는데 critic 이 못 고른다
    → 온도/신뢰영역 조절이 의미 있다

후보 32개 전부 실패로 이어진다
    → 무엇으로 골라도 안 된다. N 을 늘리거나 후보 분포 자체를 바꿔야 한다 (QAM 처럼 학습 시점)
```

### 구분하는 방법

**`oracle_select.py`** — 후보를 실제 시뮬레이터로 굴려보고 고른다. critic 불필요.
`오라클 ~100% → 문제는 순전히 critic` / `오라클 ~BC → 어떤 critic 도 못 한다`.

**제약:** 후보를 정책의 실제 32개 샘플이 아니라 **청크 하나에 노이즈를 더해** 만든다
(서버 프로토콜에 후보 전체를 받는 창구가 없다). 그래서 "π 의 멀티모달 샘플 안에 답이
있는가" 가 아니라 "청크에서 σ 이내에 더 좋은 액션이 있는가" 에 답한다.

**`oracle_walk.py` + `oracle_compare.py`** 가 네 질문에 더 가깝다 — 오라클 궤적 위를
걸으며 **실제 후보 32개를 덤프**하고 오라클의 다음 액션과 비교한다. 재는 것:
후보 구름의 산포 / 오라클이 구름 안에 있는가 / Q 와 오라클 거리의 순위상관.
**상관이 뚜렷하게 음수여야 critic 이 옳은 방향을 본다.**

이미 A 에서 잰 값이 `oracle_prefix.py` 주석에 있다:
```
t=0   |정책 청크 − 오라클| 차원당 0.030   (RTC prefix 없음)
t>=20 0.0022 ~ 0.0042                    (리플레이 붕괴 임계 0.005 미만)
```
즉 오라클 궤적 위에 있으면 정책은 오라클과 거의 같은 액션을 낸다. 차이는 첫 청크에
몰려 있다. 그런데 **첫 청크를 오라클로 깔아도 성공률이 안 올랐다** (K=0 85% / K=20 70% /
K=40 70% / K=60 80%) — "첫 청크가 승부를 정한다" 가설은 기각됐다. C 에서 같은 측정이 필요하다.
